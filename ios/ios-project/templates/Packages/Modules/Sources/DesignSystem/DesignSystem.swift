// DesignSystem
//
// Shared SwiftUI primitives for APPNAME: colors, typography, reusable
// components. Keep this module free of app-specific business logic.

import SwiftUI

/// Namespace for design-system constants.
public enum DesignSystem {
    /// Default corner radius used across cards and controls.
    public static let cornerRadius: CGFloat = 12
}
